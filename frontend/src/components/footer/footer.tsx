import React from 'react';
import { Footer as AppFooter } from '@mantine/core';

export const Footer: React.FC = () => {
    return (
        <AppFooter height={80} className="md:px-52">
            TODO Footer
        </AppFooter>
    );
};
